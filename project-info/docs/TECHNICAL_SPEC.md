# YouMind 产品深度技术拆解

## 1. 产品定位与核心价值主张

### 1.1 一句话定位
**"AI-native Research & Creation OS"** - 以AI为核心的研究与创作操作系统

### 1.2 核心价值公式
```
传统工作流:  收集 → 阅读 → 整理 → 思考 → 写作 → 输出 (线性, 6步)
YouMind:     输入 → AI处理 → 对话深化 → 可编辑输出 (闭环, 4步)
效率提升:    时间缩短 ~60%, 认知负荷降低 ~50%
```

### 1.3 目标用户分层

| 用户类型 | 占比(推测) | 核心痛点 | 使用场景 | ARPU潜力 |
|---------|-----------|---------|---------|---------|
| 学术研究者 | 25% | 文献管理混乱、论文写作困难 | 文献综述、论文撰写 | ⭐⭐⭐⭐⭐ |
| 内容创作者 | 30% | 选题困难、素材收集耗时 | 脚本写作、文案创作 | ⭐⭐⭐⭐ |
| 产品经理/分析师 | 20% | 竞品分析耗时、报告撰写 | 调研报告、PRD撰写 | ⭐⭐⭐⭐ |
| 学生群体 | 20% | 学习效率低、笔记整理难 | 课程学习、作业完成 | ⭐⭐ |
| 其他 | 5% | - | - | ⭐⭐ |

---

## 2. 信息架构 (Information Architecture)

### 2.1 核心实体关系图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User (用户)                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  Workspace   │  │  Preferences │  │  Subscription            │  │
│  │  (工作空间)   │  │  (偏好设置)   │  │  (订阅状态)              │  │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Board (项目/看板)                             │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Metadata: id, name, type, created_at, updated_at, owner_id  │  │
│  │  Type: research | collection | inspiration | custom          │  │
│  │  State: active | archived                                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
┌─────────────────────┐ ┌─────────────────────┐ ┌─────────────────────┐
│   Content (内容)     │ │   Session (会话)     │ │   Agent (AI代理)     │
│ ┌─────────────────┐ │ │ ┌─────────────────┐ │ │ ┌─────────────────┐ │
│ │ id              │ │ │ │ id              │ │ │ │ id              │ │
│ │ board_id        │ │ │ │ board_id        │ │ │ │ type            │ │
│ │ type            │ │ │ │ user_id         │ │ │ │ config          │ │
│ │ title           │ │ │ │ messages[]      │ │ │ │ state           │ │
│ │ content         │ │ │ │ created_at      │ │ │ │ context         │ │
│ │ metadata        │ │ │ └─────────────────┘ │ │ └─────────────────┘ │
│ │ source_url      │ │ │                     │ │                     │
│ │ file_info       │ │ │                     │ │                     │
│ │ ai_generated    │ │ │                     │ │                     │
│ └─────────────────┘ │ │                     │ │                     │
└─────────────────────┘ └─────────────────────┘ └─────────────────────┘
```

### 2.2 数据模型详细定义

#### User Entity
```typescript
interface User {
  id: string;                    // UUID v4
  email: string;                 // 登录邮箱
  display_name: string;          // 显示名称
  avatar_url?: string;           // 头像URL
  created_at: DateTime;          // 注册时间
  updated_at: DateTime;          // 更新时间
  
  // 订阅信息
  subscription: {
    tier: 'free' | 'pro' | 'team';  // 套餐等级
    expires_at: DateTime;           // 到期时间
    credits: number;                // 剩余积分
  };
  
  // 偏好设置
  preferences: {
    theme: 'dark' | 'light' | 'auto';
    language: string;               // 界面语言
    default_agent: string;          // 默认AI代理
    auto_save: boolean;             // 自动保存
    notifications: NotificationConfig;
  };
  
  // 使用统计
  stats: {
    total_boards: number;
    total_contents: number;
    total_sessions: number;
    ai_credits_used: number;
    last_active: DateTime;
  };
}
```

#### Board Entity
```typescript
interface Board {
  id: string;                    // UUID v4
  owner_id: string;              // 所属用户
  
  // 基本信息
  name: string;                  // 项目名称
  description?: string;          // 项目描述
  icon?: string;                 // 图标emoji或URL
  cover_image?: string;          // 封面图
  
  // 类型与状态
  type: 'research' | 'collection' | 'inspiration' | 'custom';
  state: 'active' | 'archived';
  
  // 时间戳
  created_at: DateTime;
  updated_at: DateTime;
  archived_at?: DateTime;
  
  // 协作信息
  collaborators?: Collaborator[];  // 协作者
  sharing?: SharingConfig;         // 分享设置
  
  // 统计信息
  stats: {
    content_count: number;
    session_count: number;
    last_activity: DateTime;
  };
}

type Collaborator = {
  user_id: string;
  role: 'viewer' | 'editor' | 'admin';
  added_at: DateTime;
};
```

#### Content Entity
```typescript
type ContentType = 'note' | 'link' | 'file' | 'youtube' | 'podcast' | 'pdf';

interface Content {
  id: string;                    // UUID v4
  board_id: string;              // 所属项目
  
  // 基本信息
  type: ContentType;
  title: string;
  description?: string;
  
  // 内容数据
  content?: string;              // 笔记内容（Markdown）
  
  // 来源信息
  source?: {
    url: string;                 // 原始链接
    title?: string;              // 抓取标题
    favicon?: string;            // 网站图标
    site_name?: string;          // 网站名称
    author?: string;             // 作者
    publish_date?: DateTime;     // 发布日期
    excerpt?: string;            // 摘要
    image?: string;              // 预览图
  };
  
  // 文件信息
  file?: {
    name: string;
    size: number;                // 字节
    mime_type: string;
    storage_key: string;         // 存储路径
    preview_url?: string;        // 预览URL
  };
  
  // AI处理结果
  ai_processing?: {
    summary?: string;            // AI摘要
    key_points?: string[];       // 关键点
    tags?: string[];             // AI标签
    embeddings?: number[];       // 向量嵌入
    processed_at: DateTime;
  };
  
  // 用户标注
  user_annotations?: {
    highlights: Highlight[];     // 高亮
    notes: Note[];               // 批注
    bookmarks: Bookmark[];       // 书签
  };
  
  // 时间戳
  created_at: DateTime;
  updated_at: DateTime;
  
  // 元数据
  metadata: Record<string, any>;
}

interface Highlight {
  id: string;
  text: string;
  position: { start: number; end: number };
  color: string;
  created_at: DateTime;
}

interface Note {
  id: string;
  content: string;
  position?: { start: number; end: number };
  created_at: DateTime;
  updated_at: DateTime;
}
```

---

## 3. 系统架构 (System Architecture)

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端层 (Client Layer)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Web App    │  │   iOS App    │  │Chrome Extension│  │  Browser API │    │
│  │  (React/TS)  │  │   (Swift)    │  │   (JS/TS)    │  │   (Plasmo)   │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS / WebSocket
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             网关层 (Gateway Layer)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                      API Gateway (Kong/AWS API GW)                    │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │  │
│  │  │  Rate Limit  │  │  Auth/JWT    │  │   Routing    │              │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           应用服务层 (Application Layer)                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     AI Service (AI核心服务)                          │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │  LLM Router  │  │ Skill Engine │  │ Agent System │              │   │
│  │  │  (多模型路由) │  │  (技能引擎)   │  │  (代理系统)   │              │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │ RAG Pipeline │  │  Embedding   │  │   Memory     │              │   │
│  │  │  (检索增强)   │  │   Service    │  │   System     │              │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │    Board Service    │  │   Content Service   │  │   Session Service   │ │
│  │    (项目管理)        │  │    (内容管理)        │  │    (会话管理)        │ │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘ │
│                                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │    User Service     │  │   Search Service    │  │ Notification Svc    │ │
│  │    (用户管理)        │  │    (搜索服务)        │  │    (通知服务)        │ │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           数据层 (Data Layer)                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │   PostgreSQL        │  │      Redis          │  │   Elasticsearch     │ │
│  │   (主数据库)         │  │   (缓存/会话)        │  │   (全文搜索)         │ │
│  │   - Users           │  │   - Sessions        │  │   - Content Index   │ │
│  │   - Boards          │  │   - Rate Limits     │  │   - Vector Search   │ │
│  │   - Contents        │  │   - Real-time data  │  │                     │ │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘ │
│                                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │    Vector DB        │  │    S3/MinIO         │  │   Message Queue     │ │
│  │   (Pinecone/Milvus) │  │   (对象存储)         │  │   (RabbitMQ/SQS)    │ │
│  │   - Embeddings      │  │   - Files           │  │   - Async Tasks     │ │
│  │   - Semantic Search │  │   - Images          │  │   - Event Streaming │ │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          第三方服务 (External Services)                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   OpenAI     │  │    Claude    │  │   Gemini     │  │    Grok      │    │
│  │   (GPT-4o)   │  │  (Sonnet 3.5)│  │   (Pro 1.5)  │  │   (xAI)      │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Jina AI    │  │   Firecrawl  │  │   Whisper    │  │    Stripe    │    │
│  │  (Embedding) │  │  (Web Scraping)│  │  (Transcription)│  │  (Payment)  │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心技术选型分析

| 组件 | 技术选型 | 选择理由 | 替代方案 |
|------|---------|---------|---------|
| 前端框架 | React 18 + TypeScript | 生态成熟,类型安全 | Vue3, Svelte |
| 状态管理 | Zustand + TanStack Query | 轻量,服务端状态管理 | Redux, RTK |
| UI组件 | Radix UI + Tailwind CSS | 可访问性好,定制灵活 | Ant Design |
| 后端语言 | TypeScript (Node.js) | 全栈统一,开发效率高 | Go, Python |
| 主数据库 | PostgreSQL 15 | 成熟稳定,JSON支持好 | MySQL |
| 向量数据库 | Pinecone / Milvus | 专为向量设计,高性能 | Weaviate |
| 缓存 | Redis 7 | 高性能,数据结构丰富 | Memcached |
| 搜索引擎 | Elasticsearch 8 | 全文搜索,聚合分析 | Meilisearch |
| 文件存储 | AWS S3 / MinIO | 可靠,成本可控 | GCS, Azure |
| LLM路由 | LiteLLM / 自研 | 多模型统一接口 | 直接调用各API |
| 消息队列 | RabbitMQ | 可靠,易运维 | Kafka, SQS |

---

## 4. 核心功能流程 (Core User Flows)

### 4.1 深度研究工作流

```
用户输入研究主题
    │
    ├── 系统创建 Session
    │   ├── 生成 Session ID
    │   ├── 关联 Board ID
    │   └── 初始化 Agent 配置
    │
    ├── AI 分析意图
    │   ├── 解析 / 命令
    │   ├── 确定研究范围
    │   └── 生成思考过程
    │
    ├── 信息检索 (RAG)
    │   ├── 生成 Query Embedding
    │   ├── 向量数据库相似度搜索
    │   ├── 获取相关 Content
    │   └── 计算相关性分数
    │
    ├── 外部搜索 (可选)
    │   ├── 调用搜索 API (Serper/Google)
    │   ├── 抓取网页内容
    │   └── 提取关键信息
    │
    ├── 生成研究报告
    │   ├── 整合所有 Sources
    │   ├── 调用 LLM 生成报告
    │   ├── 结构化输出
    │   └── 添加引用标注
    │
    ├── 保存结果
    │   ├── 创建 Content (type=note)
    │   ├── 关联 Session ID
    │   └── 标记 ai_generated=true
    │
    └── 返回给用户
        ├── 显示研究正文
        ├── 可展开思考过程
        └── 提供追问输入框
```

### 4.2 内容保存工作流

```
用户触发保存
    │
    ├── 方式1: 浏览器扩展
    │   ├── 用户在任意网页点击扩展图标
    │   ├── 扩展抓取: URL, Title, Content, Images
    │   ├── 发送到: POST /api/content/quick-add
    │   └── 返回: 保存成功 + 预览卡片
    │
    ├── 方式2: 链接粘贴
    │   ├── 用户在看板粘贴URL
    │   ├── 后端调用: Firecrawl / Jina AI Reader
    │   ├── 抓取并解析网页内容
    │   ├── 生成摘要和标签 (AI)
    │   └── 保存为 Content(type="link")
    │
    ├── 方式3: 文件上传
    │   ├── 用户拖拽文件到看板
    │   ├── 前端直传到: S3 (Presigned URL)
    │   ├── 后端异步处理队列:
    │   │   ├── PDF: 文本提取 (PyPDF/OCR)
    │   │   ├── 图片: OCR识别 (Tesseract/AWS Textract)
    │   │   ├── 音频: 转录 (Whisper API)
    │   │   └── 视频: 提取音频 → 转录
    │   ├── 生成向量嵌入 (Embedding API)
    │   └── 存入向量数据库
    │
    └── 方式4: 手动创建笔记
        ├── 用户点击"新建笔记"
        ├── 打开富文本编辑器 (Tiptap/Editor.js)
        ├── 支持: Markdown, 富文本, 代码块, 图片
        ├── 实时自动保存 (Debounce 2s)
        └── 保存为 Content(type="note")
```

### 4.3 AI对话状态机

```
[Idle] ──用户输入──> [Analyzing Intent]
                         │
                         ├── 检测到 /命令 ──> [Skill Execution]
                         │                         │
                         │                         ▼
                         │                   [Parameter Collection]
                         │                         │
                         │                         ▼
                         │                   [Tool Calling]
                         │                         │
                         │                    (Web Search / File Read / etc)
                         │                         │
                         │                         ▼
                         │                   [Response Generation]
                         │                         │
                         └─────────────────────────┤
                                                   │
                                                   ▼
                                            [Streaming Response]
                                                   │
                              ┌────────────────────┼────────────────────┐
                              │                    │                    │
                              ▼                    ▼                    ▼
                        [User Followup]    [Content Created]    [Session End]
                              │                    │                    │
                              └────────────────────┴────────────────────┘
                                                   │
                                                   ▼
                                              [Save to DB]
                                                   │
                                                   ▼
                                              [Return to Idle]
```

---

## 5. UI/UX 设计规范（精确规格）

### 5.1 三栏布局详细规格
```
┌────────────────────────────────────────────────────────────────────────────┐
│                              视口: 100vw × 100vh                           │
├──────────────┬──────────────────────────────────┬──────────────────────────┤
│   Sidebar    │          Main Content            │      Chat Panel          │
│   (固定)     │         (自适应)                  │       (固定)             │
│   260px      │         flex: 1                   │       400px              │
│              │         min-width: 600px          │       min-width: 360px   │
│              │         max-width: 1200px         │       max-width: 480px   │
├──────────────┼──────────────────────────────────┼──────────────────────────┤
│  Logo 48×48  │  Header 64px                     │  AI Avatar 72px          │
│  Navigation  │  Content Area                    │  Messages (scroll)       │
│  - Projects  │  - Grid / List                   │  - Auto-scroll bottom    │
│  - Skills    │  - Cards / Empty                 │                          │
│  User 72px   │  Action Bar 80px                 │  Input Area 140px        │
└──────────────┴──────────────────────────────────┴──────────────────────────┘
```

### 5.2 颜色系统
```css
:root {
  --bg-primary: #0f0f0f;
  --bg-secondary: #141414;
  --bg-tertiary: #1a1a1a;
  --bg-hover: #252525;
  
  --text-primary: #ffffff;
  --text-secondary: rgba(255,255,255,0.65);
  --text-tertiary: rgba(255,255,255,0.45);
  
  --border-primary: #2a2a2a;
  --border-hover: #404040;
  
  --accent-primary: #6366f1;
  --accent-secondary: #8b5cf6;
  --accent-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
}
```

---

## 6. 技能系统详细设计

### 6.1 技能数据结构
```typescript
interface Skill {
  id: string;
  name: string;
  category: SkillCategory;
  description: string;
  icon: string;
  triggers: { command?: string; keyword?: string[] };
  system_prompt: string;  // 核心！
  parameters: {
    temperature?: number;
    max_tokens?: number;
    tools?: string[];
  };
  is_official: boolean;
  is_custom: boolean;
  usage_count: number;
  rating?: number;
}
```

### 6.2 技能示例：深度研究
```typescript
const deepResearchSkill: Skill = {
  id: "skill-deep-research-v1",
  name: "深度研究",
  category: "research",
  description: "针对特定主题进行深度调研，生成结构化研究报告",
  icon: "🔬",
  triggers: {
    command: "/research",
    keyword: ["研究", "调研", "分析"]
  },
  system_prompt: `你是一位专业的研究分析师。你的任务是：
1. 理解用户的研究主题和意图
2. 检索相关的已有资料
3. 必要时进行网络搜索获取最新信息
4. 整合信息，生成结构化的研究报告
5. 报告应包含：执行摘要、核心发现、详细分析、数据来源、后续建议

输出格式：Markdown，支持标题、列表、表格、引用。`,
  parameters: {
    temperature: 0.3,
    max_tokens: 8000,
    tools: ["web_search", "vector_search", "file_reader"]
  }
};
```

---

## 7. 关键技术实现细节

### 7.1 RAG (检索增强生成) 实现

```
用户Query
    │
    ▼
[Embedding Model] 
    │ (text-embedding-3-large / bge-m3)
    ▼
Query Vector (1536 dimensions)
    │
    ├───> [Vector DB Similarity Search] (cosine similarity)
    │          │
    │          ▼
    │     Top-K Results (K=5-10)
    │          │
    │          ▼
    │     Reranking (Cohere Rerank / 自研)
    │          │
    │          ▼
    │     Filter by Board / Time / Type
    │          │
    │          ▼
    │     Retrieved Contexts
    │
    ▼
[Prompt Construction]
    │
    ├── System Prompt (Skill定义)
    ├── User Query
    ├── Retrieved Contexts
    └── Conversation History
    │
    ▼
[LLM Generation]
    │
    ▼
Response + Citations
```

### 7.2 实时协作实现

```
WebSocket 连接管理
    │
    ├── 用户A编辑 Content
    │   ├── 前端发送: ws.send({type: 'edit', content_id, changes, version})
    │   ├── 服务端广播: ws.broadcast(content_id, {type: 'remote_edit', changes})
    │   ├── 其他用户接收并应用 Operational Transform
    │   └── 冲突解决: 使用 Yjs / Automerge 算法
    │
    ├── Cursor Position 同步
    │   ├── 节流: 50ms
    │   ├── 广播: {user_id, cursor_pos, selection_range}
    │   └── 渲染: 不同颜色的光标
    │
    └── 在线状态
        ├── Presence: 心跳 30s
        ├── 显示: "3人在线编辑"
        └── 头像列表
```

### 7.3 文件处理 Pipeline

```
文件上传
    │
    ▼
S3 Direct Upload (Presigned URL)
    │
    ▼
S3 Event Trigger → SQS Queue
    │
    ▼
Worker 处理:
    │
    ├── PDF:
    │   ├── PyPDF2 / pdfplumber 提取文本
    │   ├── OCR (Tesseract / AWS Textract) 处理扫描件
    │   └── 生成预览图 (ImageMagick)
    │
    ├── Image:
    │   ├── OCR 提取文字
    │   ├── 生成缩略图 (Sharp)
    │   └── 图像标签识别 (AWS Rekognition)
    │
    ├── Audio/Video:
    │   ├── 提取音频 (FFmpeg)
    │   ├── Whisper API 转录
    │   └── 生成字幕文件
    │
    └── Office:
        ├── LibreOffice 转换为 PDF
        └── 按 PDF 流程处理
    │
    ▼
生成 Embedding (OpenAI / Jina AI)
    │
    ▼
存入 Vector DB (Pinecone)
    │
    ▼
更新 Content 状态: processed=true
```

---

## 8. API 设计 (RESTful + WebSocket)

### 8.1 核心端点

#### Boards API
```
GET    /api/v1/boards              # 列表 (支持分页、筛选)
POST   /api/v1/boards              # 创建
GET    /api/v1/boards/:id          # 详情
PATCH  /api/v1/boards/:id          # 更新
DELETE /api/v1/boards/:id          # 删除
POST   /api/v1/boards/:id/archive  # 归档
```

#### Contents API
```
GET    /api/v1/boards/:id/contents     # 看板内容列表
POST   /api/v1/contents                # 创建内容
GET    /api/v1/contents/:id            # 详情
PATCH  /api/v1/contents/:id            # 更新
DELETE /api/v1/contents/:id            # 删除
POST   /api/v1/contents/:id/annotate   # 添加批注
```

#### Sessions API
```
POST   /api/v1/sessions                    # 创建会话
GET    /api/v1/sessions/:id                # 获取会话
POST   /api/v1/sessions/:id/messages       # 发送消息 (流式返回)
GET    /api/v1/sessions/:id/messages       # 获取消息历史
DELETE /api/v1/sessions/:id                # 删除会话
```

#### AI API
```
POST   /api/v1/ai/chat              # 通用对话
POST   /api/v1/ai/skills/:id/invoke # 调用特定技能
POST   /api/v1/ai/summarize         # 摘要
POST   /api/v1/ai/search            # 语义搜索
```

### 8.2 WebSocket 事件

```javascript
// Client -> Server
{
  "type": "join_board",
  "board_id": "xxx"
}

{
  "type": "cursor_move",
  "content_id": "xxx",
  "position": { "x": 100, "y": 200 }
}

{
  "type": "content_edit",
  "content_id": "xxx",
  "operation": {...}, // OT operation
  "version": 5
}

// Server -> Client
{
  "type": "presence_update",
  "board_id": "xxx",
  "users": [...]
}

{
  "type": "remote_cursor",
  "user_id": "xxx",
  "position": { "x": 100, "y": 200 },
  "color": "#6366f1"
}

{
  "type": "remote_edit",
  "content_id": "xxx",
  "operation": {...}
}
```

---

## 9. 安全与隐私设计

### 9.1 数据隔离
- 用户数据物理隔离 (Row-level Security in PostgreSQL)
- Board 级别访问控制 (viewer/editor/admin)
- API 请求严格校验用户权限

### 9.2 AI 数据处理
- 用户数据不用于模型训练 (Zero-data retention with OpenAI)
- 敏感内容本地处理选项
- 向量数据加密存储

### 9.3 内容安全
- 上传文件病毒扫描 (ClamAV)
- 恶意链接检测
- 敏感信息过滤 (PII detection)

---

## 10. 性能优化策略

### 10.1 前端优化
- 虚拟滚动 (内容列表 > 1000 项)
- 图片懒加载 + WebP 格式
- 代码分割 (Route-based)
- Service Worker 缓存

### 10.2 后端优化
- 数据库连接池 (PgBouncer)
- Redis 缓存热点数据
- 异步处理非关键路径
- LLM 响应流式输出

### 10.3 向量搜索优化
- HNSW 索引
- 向量量化 (Scalar/Product quantization)
- 预过滤 + 后重排序

---

## 11. 部署架构

### 11.1 生产环境
```
CDN (CloudFront) -> LB (ALB) -> K8s Cluster
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
              Web Pods          API Pods         Worker Pods
              (Next.js)        (Node.js)         (Bull Queue)
                                     │
                                     ▼
                            ┌─────────────────┐
                            │  RDS PostgreSQL │
                            │  ElastiCache    │
                            │  OpenSearch     │
                            │  S3             │
                            └─────────────────┘
```

### 11.2 CI/CD Pipeline
```
Git Push -> GitHub Actions -> Build -> Test -> Deploy Staging
                                               │
                                               ▼
                                          E2E Tests
                                               │
                                               ▼
                                          Deploy Production
                                               │
                                               ▼
                                          Smoke Tests
```

---

**文档版本:** v1.0  
**最后更新:** 2026-02-18  
**作者:** AI Assistant
